.DS_Store
.idea
*.log
tmp/
.direnv

*.py[cod]
__pypackages__
*.egg
build
htmlcov
.pdm.toml

.terraform*
terraform.tfstate*
.nixpacks
kubeconfig.yaml

# Ignore all files inside .dapr/components
.dapr/components/*

# Whitelist files with "enc" in the filename
!.dapr/components/*enc*

# Whitelist the local secret store component
!.dapr/components/local-secret-store.yaml

!.dapr/components/redis-pubsub.yaml

# Secrets directory (actual secrets, not the example)
secrets/secrets.json

# Ignore chart packages
my-chart/charts/helmet-0.7.0.tgz

# Ignore all YAML files in the terraform directory except Pulumi.yaml
terraform/*.yaml 
!terraform/Pulumi.yaml
.pdm-python
