You are working in a git repo at the current directory.

## Task
Create a file `tests/test_pipeline_exp_2.md` with the following content:

```markdown
# Pipeline Experiment 2 — 5 minute test

This file was created by an ACP worker to test the autonomous merge pipeline.

## Verification
- Created at: [current timestamp]
- Worker: ACP experiment 2
- Purpose: Verify poller detects ACP worker completion within 60s
```

Then commit with message: "test: pipeline experiment 2 - ACP worker 5min test"

Push to origin.

Do NOT modify any other files.
